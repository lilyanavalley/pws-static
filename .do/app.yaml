alerts:
- rule: DEPLOYMENT_FAILED
- rule: DOMAIN_FAILED
features:
- buildpack-stack=ubuntu-22
ingress:
  rules:
  - component:
      name: harrisii
    match:
      path:
        prefix: /
name: harrisii
region: tor
static_sites:
- environment_slug: html
  github:
    branch: live
    deploy_on_push: true
    repo: lilyanavalley/pws-static
  name: harrisii
  source_dir: /
